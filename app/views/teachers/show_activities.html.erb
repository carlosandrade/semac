<p>
  <b>Group:</b>
  <%=h @group.year %>.<%=h @group.semester %>  <%=h @group.number %>
  </ br>
</p>

<p>
  <b>Activities:</b>
<table>
  <tr>
    <th>Title</th>
    <th>Delivery Date</th>
    <th>Specification</th>
  </tr>
  <tr>
    <% @group.activities.each do |a| %>
      <td><%= link_to a.title, :action => 'activity_submissions', :id => a.id %> </td>
      <td><%=h a.delivery_date %></td>
      <td><%=h a.specification %></td>
    </tr>
  <% end %>
</table>
</p>
<%= link_to 'Back', :action => "manage_group", :id => @group %> 
